# Export Plain String Typography Utility

The package exports a plain string typography utility alongside the React boundary, even though the initial React component specification only listed it as optional. This gives callers an explicit way to typograph strings used in props or preprocessing without encouraging TextWithTypography to rewrite arbitrary React props behind the user's back.
