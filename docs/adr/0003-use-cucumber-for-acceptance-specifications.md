# Use Cucumber for Acceptance Specifications

The project uses Cucumber feature files for behavior-level acceptance coverage of TextWithTypography instead of only behavior-named unit tests. This adds a dedicated BDD layer, but it makes the public component contract readable as product scenarios and keeps lower-level unit tests focused on pure helpers and edge cases.
